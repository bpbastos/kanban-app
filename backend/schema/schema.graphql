# schema.graphql
extend type Mutation {
  addTask(title: String!, workflowId: ID!, boardId: ID!): Task! @resolve
  removeTask(taskId: ID!): Task! @resolve
  addSubTask(title: String!, taskId: ID!): SubTask! @resolve
  markSubTaskDone(subTaskId: ID!): SubTask! @resolve
}